<style>
  div {
    width: 4rem;
    height: 4rem;
    border-radius: 50%;
    border: 6px dashed var(--primary);
    animation: rotate 5s linear infinite;
  }

  @keyframes rotate {
    to {
      transform: rotateZ(1turn);
    }
  }
</style>

<div />
